# ──────────────────────────────────────────────────────────────
#  client/.env.example
#
#  Copy this file to  client/.env  and adjust if your backend
#  runs on a different port or host.
#  NEVER commit  client/.env  — it is listed in  .gitignore.
# ──────────────────────────────────────────────────────────────

# Base URL for all API requests made by the React frontend.
# Must point to the running Express server.
VITE_API_URL=http://localhost:5000/api
